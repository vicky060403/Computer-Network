# 📧 Email Sending Application (SMTP Simulation)

This project is a Python-based email sending simulation using the **SMTP protocol**.  
It allows users to send emails with attachments using their Gmail account securely.

---

## 🚀 Features
- User login authentication  
- Send plain text or HTML emails  
- Add file attachments (PDF, CSV, etc.)  
- Uses Gmail SMTP for delivery  

---

## 🧠 Requirements
- Python 3.x  
- Internet connection  
- Required libraries:
  ```bash
  pip install smtplib email
